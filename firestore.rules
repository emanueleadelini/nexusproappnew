rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGOLE TEMPORANEE PER DEBUG ESTREMO
    // Permettono l'accesso completo a qualsiasi utente autenticato.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}